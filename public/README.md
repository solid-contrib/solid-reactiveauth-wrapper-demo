This folder contains static assets used by the application.

Its contents are served by webpack on [localhost](http://localhost:8080/) after running `npm start` in the [root folder](..).

The entrypoint is [homepage](./index.html).
